# Link Auto-Ingest Rule

At the start of every session, check `~/link/raw/` for files that don't have a corresponding source page in `~/link/wiki/sources/`. If any unprocessed files are found, list them and offer to ingest them following the protocol in `~/link/LINK.md`.
