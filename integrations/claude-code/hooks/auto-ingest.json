{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Create",
        "hooks": [
          {
            "type": "command",
            "command": "CHANGED=$(echo \"$TOOL_INPUT\" | grep -o '~/link/raw/[^ ]*' 2>/dev/null); [ -n \"$CHANGED\" ] && echo '{\"hookSpecificOutput\":{\"additionalContext\":\"Link: New file in raw/. Run the ingest protocol from LINK.md — create source page, update concepts, entities, index, and log.\"}}' || true"
          }
        ]
      }
    ]
  }
}
