## Link — Personal Knowledge Wiki

A personal knowledge wiki lives at `~/link/`. It has raw sources in `~/link/raw/` and compiled wiki pages in `~/link/wiki/`.

When the user says **"ingest"**, **"query"**, **"lint"**, or **"research"**, read `~/link/LINK.md` for instructions and follow the protocol. Use terminal commands to access `~/link/` since it's outside the workspace.

Otherwise, don't interfere — just be a normal assistant.
